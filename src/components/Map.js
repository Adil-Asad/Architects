import styles from "@/styles/map.module.css";

const Hero = () => {
  return (
    <div className={styles.heroContainer}>
      <img className={styles.banner} src="/assets/images/map.jpg" alt="map" />
    </div>
  );
};

export default Hero;
